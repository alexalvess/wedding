"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CLI = void 0;
const tslib_1 = require("tslib");
require("reflect-metadata");
const fs_extra_1 = tslib_1.__importDefault(require("fs-extra"));
const path_1 = tslib_1.__importDefault(require("path"));
const cp = tslib_1.__importStar(require("child_process"));
const semver = tslib_1.__importStar(require("semver"));
const inversify_1 = require("inversify");
const dependency_register_1 = require("./dependency-register");
class CLI {
    static get nimbleVersion() {
        if (!this._nimbleVersion) {
            const prefix = 'npm show @nimble-ts/core';
            const stable = cp.execSync(`${prefix} version`).toString('utf8').replace(/(\r\n|\n|\r)/gm, '');
            const beta = cp.execSync(`${prefix} dist-tags.beta`).toString('utf8').replace(/(\r\n|\n|\r)/gm, '');
            const alpha = cp.execSync(`${prefix} dist-tags.alpha`).toString('utf8').replace(/(\r\n|\n|\r)/gm, '');
            const equalOrGreater = (v1, v2) => {
                v1 = v1.split('-')[0];
                v2 = v2.split('-')[0];
                return v1 === v2 || semver.gt(v1, v2);
            };
            if (alpha && equalOrGreater(alpha, stable) && (!beta || equalOrGreater(alpha, beta))) {
                this._nimbleVersion = alpha;
            }
            else if (beta && equalOrGreater(beta, stable) && (!alpha || equalOrGreater(beta, alpha))) {
                this._nimbleVersion = beta;
            }
            else {
                this._nimbleVersion = stable;
            }
        }
        return this._nimbleVersion;
    }
    static get version() { return this.package ? this.package.version : '1.0.0'; }
    static get globalVersion() {
        if (this.workingInLocalProject()) {
            try {
                if (process.mainModule) {
                    let globalPath = path_1.default.join(process.mainModule['path'], '../package.json');
                    if (fs_extra_1.default.existsSync(globalPath)) {
                        let packgeJson = JSON.parse(fs_extra_1.default.readFileSync(globalPath, 'utf8'));
                        return packgeJson ? packgeJson.version : this.version;
                    }
                }
            }
            catch (_a) {
                return this.version;
            }
        }
        return this.version;
    }
    static get worksPath() { return process.cwd(); }
    static start() {
        this.getInformation();
        dependency_register_1.DependencyRegister.register(this.container);
        this.nb = this.container.get('NB');
        this.nb.setArgurments(process.argv.slice(2));
        this.nb.start();
    }
    static inject(type) {
        return this.container.get(type);
    }
    static hasGlobalInstalled() {
        if (this.workingInLocalProject()) {
            try {
                let packgeJson = require("../package.json");
                return true;
            }
            catch (_a) {
                return false;
            }
        }
        return true;
    }
    static workingInLocalProject() {
        let projectRootPath = path_1.default.join(__dirname, '../../../../');
        return fs_extra_1.default.existsSync(`${projectRootPath}/nimble.json`);
    }
    static worksPathIsRootProject(path) {
        if (!path)
            path = this.worksPath;
        if (!path.endsWith('/'))
            path += '/';
        if (!fs_extra_1.default.pathExistsSync(`${path}src`) || !fs_extra_1.default.existsSync(`${path}package.json`)) {
            return false;
        }
        if (!fs_extra_1.default.existsSync(`${path}nimble.json`)) {
            return false;
        }
        let packageFile = null;
        try {
            packageFile = require(`${path}package.json`);
        }
        catch (_a) { }
        if (!packageFile || !packageFile['dependencies'] || !packageFile['dependencies']['@nimble-ts/core'])
            return false;
        return true;
    }
    static worksPathIsSourceDir() {
        let dirName = this.worksPath.split(path_1.default.sep).pop();
        return dirName === 'src';
    }
    static isNimbleProject() {
        let parent = this.worksPath;
        let rootPath = path_1.default.parse(parent).root;
        while (parent && parent !== rootPath) {
            if (this.worksPathIsRootProject(parent))
                return true;
            parent = this.getParentPath(parent);
        }
        return false;
    }
    static getNimbleProjectRootPath() {
        let parent = this.worksPath;
        let rootPath = path_1.default.parse(parent).root;
        while (parent && parent !== rootPath) {
            if (this.worksPathIsRootProject(parent))
                return parent;
            parent = this.getParentPath(parent);
        }
        return parent;
    }
    static getParentPath(completePath) {
        let parentPath = path_1.default.join(completePath + '/..');
        return parentPath;
    }
    static getInformation() {
        try {
            this.package = require('package.json');
        }
        catch (_a) {
            this.package = require('../package.json');
        }
    }
}
CLI.container = new inversify_1.Container();
exports.CLI = CLI;
CLI.start();
//# sourceMappingURL=cli.js.map
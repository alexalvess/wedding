"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BaseInstruction = void 0;
const util_1 = require("util");
class BaseInstruction {
    constructor(instruction) {
        Object.assign(this, instruction);
    }
    completePath() {
        let parents = this.getParents().reverse().map(x => x.name);
        let path = parents.join('/') + '/' + this.name;
        path = path.replace(/\/\/\//g, '/').replace(/\/\//g, '/');
        return path;
    }
    getParents() {
        let parents = [];
        let parent = this.parent;
        while (!(0, util_1.isNullOrUndefined)(parent)) {
            parents.push(parent);
            parent = parent.parent;
        }
        return parents;
    }
}
exports.BaseInstruction = BaseInstruction;
//# sourceMappingURL=base-instruction.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.FileCreator = void 0;
const tslib_1 = require("tslib");
const fs_extra_1 = tslib_1.__importDefault(require("fs-extra"));
const file_instruction_1 = require("./file-instruction");
const directory_instruction_1 = require("./directory-instruction");
class FileCreator {
    constructor() {
        this.filesInstruction = [];
    }
    initialize(filesInstruction) {
        this.filesInstruction = this.prepareFilesInstructions(filesInstruction);
    }
    startCreateFiles(callback) {
        return tslib_1.__awaiter(this, void 0, void 0, function* () {
            this.callbackOnCreateFile = callback;
            if (this.filesInstruction.length > 0) {
                yield this.createFilesInstruction(this.filesInstruction);
            }
        });
    }
    prepareFilesInstructions(filesInstruction, parent) {
        return filesInstruction.map((x) => {
            let instruction = ('content' in x || 'from' in x) ? new file_instruction_1.FileInstruction(x) : new directory_instruction_1.DirectoryInstruction(x);
            instruction.parent = parent;
            if (instruction instanceof directory_instruction_1.DirectoryInstruction)
                instruction.children = this.prepareFilesInstructions(instruction.children, instruction);
            return instruction;
        });
    }
    createFilesInstruction(files) {
        return tslib_1.__awaiter(this, void 0, void 0, function* () {
            for (let file of files) {
                let worksPath = process.cwd();
                let filePath = worksPath + '/' + file.completePath();
                if (file instanceof directory_instruction_1.DirectoryInstruction) {
                    if (!worksPath.endsWith(file.completePath())) {
                        if (!fs_extra_1.default.existsSync(filePath)) {
                            try {
                                yield fs_extra_1.default.mkdir(filePath);
                            }
                            catch (e) {
                                if (this.callbackOnCreateFile)
                                    this.callbackOnCreateFile({ fileInstruction: file, error: e });
                            }
                        }
                    }
                    if (file.children && file.children.length > 0)
                        yield this.createFilesInstruction(file.children);
                }
                else if (file instanceof file_instruction_1.FileInstruction) {
                    try {
                        if (file.content)
                            yield fs_extra_1.default.writeFile(filePath, file.content);
                        else if (file.from)
                            fs_extra_1.default.copyFile(file.from, filePath);
                        if (this.callbackOnCreateFile)
                            this.callbackOnCreateFile({ fileInstruction: file });
                    }
                    catch (e) {
                        if (this.callbackOnCreateFile)
                            this.callbackOnCreateFile({ fileInstruction: file, error: e });
                    }
                }
            }
        });
    }
}
exports.FileCreator = FileCreator;
//# sourceMappingURL=file-creator.js.map
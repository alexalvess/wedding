import { Type } from './type.interface';
import 'reflect-metadata';
export declare const INJECTABLE_METADATA_KEY: unique symbol;
export declare function Injectable(config?: ProviderConfig): (target: any) => any;
export declare function isInjectable<T>(target: Type<T>): boolean;
export declare class ProviderConfig {
    /**Creates only one instance to use for all injections */
    single: boolean;
}

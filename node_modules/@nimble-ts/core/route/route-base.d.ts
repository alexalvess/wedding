import { Page } from '../page/page';
import { Type } from '../inject/type.interface';
import { RouteGuard } from './route-guard';
export declare class RouteBase {
    path: string;
    guard?: Type<RouteGuard>[];
    data?: {
        [key: string]: any;
    };
    redirect?: string;
    page?: (() => Promise<any>) | Type<Page> | string;
    children?: RouteBase[];
    constructor(route?: Partial<RouteBase>);
}
export declare class InjectedPage {
    template: string;
    constructor(options: Partial<InjectedPage>);
}

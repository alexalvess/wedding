"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.RouteRender = void 0;
const injectable_1 = require("../inject/injectable");
const render_abstract_1 = require("../render/render-abstract");
const listeners_collector_1 = require("../providers/listeners-collector");
const render_helper_1 = require("../render/render-helper");
const router_1 = require("./router");
const header_render_1 = require("../render/header-render");
let RouteRender = class RouteRender extends render_abstract_1.RenderAbstract {
    constructor(headerRender, listenersCollector) {
        super(listenersCollector);
        this.headerRender = headerRender;
        this.listenersCollector = listenersCollector;
    }
    /**
     * Prepare a specific route to be compiled and rendered
     * @param route
     */
    prepareRouteToCompileAndRender(route) {
        var _a;
        return __awaiter(this, void 0, void 0, function* () {
            let childRoutes = [route, ...route.getAllParents()];
            childRoutes.reverse();
            let commonParentRoute = router_1.Router.previous ? router_1.Router.getCommonParentOfTwoRoutes(route, router_1.Router.previous) : null;
            for (let route of childRoutes) {
                const isChildOfCommonParent = (_a = commonParentRoute === null || commonParentRoute === void 0 ? void 0 : commonParentRoute.isChild(route, true)) !== null && _a !== void 0 ? _a : true;
                if (!commonParentRoute || isChildOfCommonParent) {
                    this.createElementFromStructure(route.structuredTemplate);
                    yield route.pageInstance.onEnter();
                }
            }
        });
    }
    /**
     * Compile and render a specific route and your children
     * @param route
     */
    compileAndRenderRoute(route) {
        var _a, _b, _c;
        let childRoutes = [route, ...route.getAllParents()].reverse();
        let tallestRoute = childRoutes.shift();
        let commonParentRoute = router_1.Router.previous ? router_1.Router.getCommonParentOfTwoRoutes(route, router_1.Router.previous) : null;
        if (commonParentRoute && router_1.Router.previous) {
            const fromPreviousToHighestParent = [router_1.Router.previous, ...router_1.Router.previous.getAllParents()];
            for (let child of fromPreviousToHighestParent) {
                if (child !== commonParentRoute) {
                    (_b = (_a = child.structuredTemplate) === null || _a === void 0 ? void 0 : _a.compiledNode) === null || _b === void 0 ? void 0 : _b.remove();
                }
                else {
                    break;
                }
            }
            ;
        }
        if ((_c = commonParentRoute === null || commonParentRoute === void 0 ? void 0 : commonParentRoute.structuredTemplate) === null || _c === void 0 ? void 0 : _c.nodeIsRenderedInDOM) {
            commonParentRoute.structuredTemplate.removeCompiledNode();
        }
        this.headerRender.resolveTitleAndMetaTags(tallestRoute);
        this.listenersCollector.unsubscribeAll();
        render_helper_1.RenderHelper.removeAllChildrenOfElement(this.app.rootElement);
        let routeRootElement = this.compileElementFromStructure(tallestRoute.structuredTemplate);
        let parent = routeRootElement;
        for (let childRoute of childRoutes) {
            let routerElement = this.getRouterElement(parent);
            if (routerElement) {
                let child = this.compileElementFromStructure(childRoute.structuredTemplate);
                routerElement.appendChild(child);
                parent = child;
            }
            this.headerRender.resolveTitleAndMetaTags(childRoute);
        }
        this.app.rootElement.appendChild(routeRootElement);
    }
    /**
     * Render again a specific route and your children
     * @param route
     */
    rerenderRoute(route) {
        let childRoutes = [route, ...route.getAllParents()].reverse();
        let parent = null;
        for (let child of childRoutes) {
            if (!parent || child.structuredTemplate.isRendered) {
                this.recompileElementFromStructure(child.structuredTemplate);
            }
            else {
                let routerElement = this.getRouterElement(parent);
                if (routerElement) {
                    let childNode = this.compileElementFromStructure(child.structuredTemplate);
                    routerElement.appendChild(childNode);
                    parent = childNode;
                }
            }
            parent = child.structuredTemplate.compiledNode;
        }
        // this.listenersCollector.applyAllListeners();
    }
    /**
     * Notifies the "onDestroy()" methods of pages unrendereds and "onInit()" methods of pages already rendered
     * @param route
     */
    notifyRoutesAfterRouteChanged(route) {
        return __awaiter(this, void 0, void 0, function* () {
            let previousRoute = router_1.Router.previous;
            let highestParentRoute = route.getHighestParentOrHimself();
            let commonParentRoute = previousRoute ? router_1.Router.getCommonParentOfTwoRoutes(route, previousRoute) : highestParentRoute;
            yield this.notifyOldRoutesElementDestroyed(commonParentRoute, previousRoute);
            const isRendered = yield this.notifyNewRoutesElementRendered(commonParentRoute, highestParentRoute, route);
            return { isRendered };
        });
    }
    /**
     * Notifies the "onInit()" methods of pages already rendered
     * @param route
     */
    notifyRoutesAfterRerender(route) {
        return __awaiter(this, void 0, void 0, function* () {
            let previousRoute = router_1.Router.previous;
            let highestParentRoute = route.getHighestParentOrHimself();
            let commonParentRoute = previousRoute ? router_1.Router.getCommonParentOfTwoRoutes(route, previousRoute) : highestParentRoute;
            const isRendered = yield this.notifyNewRoutesElementRendered(commonParentRoute, highestParentRoute, route, true);
            return { isRendered };
        });
    }
    /**
     * Notifies the "onAfterInit()" methods of pages after rendered
     * @param route
     */
    notifyRoutesAfterInit(route) {
        return __awaiter(this, void 0, void 0, function* () {
            let allRoutes = [route, ...route.getAllParents()];
            for (let route of allRoutes.reverse()) {
                if (!route.pageInstance.isAfterInitialized) {
                    route.pageInstance.isAfterInitialized = true;
                    yield route.pageInstance.onAfterInit();
                }
            }
            ;
        });
    }
    notifyOldRoutesElementDestroyed(commonParentRoute, previousRoute) {
        return __awaiter(this, void 0, void 0, function* () {
            if (previousRoute) {
                let onlyOldRoutesRemoved = [];
                for (let route of [previousRoute, ...previousRoute.getAllParents()]) {
                    if (route === commonParentRoute)
                        break;
                    onlyOldRoutesRemoved.push(route);
                }
                for (let route of onlyOldRoutesRemoved.reverse()) {
                    if (!route.pageInstance.isDestroyed) {
                        route.pageInstance.isDestroyed = true;
                        yield route.pageInstance.onDestroy();
                    }
                }
            }
        });
    }
    /**
     * Notifies the "onInit()" methods of pages already rendered
     * @param commonParentRoute
     * @param highestParentRoute
     * @param route
     * @param inRerender
     */
    notifyNewRoutesElementRendered(commonParentRoute, highestParentRoute, route, inRerender = false) {
        return __awaiter(this, void 0, void 0, function* () {
            let onlyNewRoutesRendered = [];
            let allRoutes = [route, ...route.getAllParents()];
            if (!inRerender && commonParentRoute !== highestParentRoute && highestParentRoute !== route) {
                for (let route of allRoutes) {
                    if (route === commonParentRoute)
                        break;
                    onlyNewRoutesRendered.push(route);
                }
            }
            else
                onlyNewRoutesRendered = allRoutes;
            let isRendered = true;
            let init = 0;
            for (let route of onlyNewRoutesRendered.reverse()) {
                isRendered = route.structuredTemplate.isRendered;
                if (!route.pageInstance.isInitialized && isRendered) {
                    if (route.path === '{id}') {
                        // console.log('RUN onInit', route.path, inRerender ? '(rerender)' : '(changed)');
                        init = performance.now();
                    }
                    route.pageInstance.isInitialized = true;
                    yield route.pageInstance.onInit();
                }
            }
            ;
            // if (init > 0) console.log('RUN onInit', performance.now() - init, inRerender ? '(rerender)' : '(changed)');
            return isRendered;
        });
    }
    getRouterElement(parent) {
        return parent ? parent.querySelector('nimble-router') : null;
    }
};
RouteRender = __decorate([
    injectable_1.Injectable({ single: true }),
    __metadata("design:paramtypes", [header_render_1.HeaderRender,
        listeners_collector_1.ElementListenersCollector])
], RouteRender);
exports.RouteRender = RouteRender;
//# sourceMappingURL=route-render.js.map
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FormField = void 0;
const app_1 = require("../app");
const listeners_collector_1 = require("../providers/listeners-collector");
const observer_1 = require("../core/observer");
const util_1 = require("util");
class FormField {
    constructor(obj) {
        this._elements = [];
        this._valueChanges = new observer_1.Observer();
        this._errors = null;
        this._validators = [];
        this._isEnabled = true;
        this._blurred = false;
        this._touched = false;
        Object.assign(this, obj);
    }
    get elements() { return this._elements; }
    get value() { return this._value; }
    get valueChanges() { return this._valueChanges; }
    get errors() { return this._errors; }
    get validators() { return this._validators; }
    get isEnabled() { return this._isEnabled; }
    get isDisabled() { return !this._isEnabled; }
    /** Indicates that you have already entered the field and left. */
    get blurred() { return this._blurred; }
    /** Indicates that you have already entered the field. */
    get touched() { return this._touched; }
    /**Check if all validators is valid and returns a boolean */
    get isValid() { return !this.errors || Object.keys(this.errors).filter(x => !(this.errors[x] === false)).length === 0; }
    get listenerCollector() { return app_1.NimbleApp.inject(listeners_collector_1.ElementListenersCollector); }
    setValue(value, options = {}) {
        var _a, _b;
        let interacted = false;
        this._value = value;
        if ((!options || !options.noUpdateElement) && this.elements.length === 1) {
            let element = this.elements[0];
            if (element.type !== 'checkbox' && element.type !== 'radio') {
                element.value = value;
                if (element.value === value) {
                    element.dispatchEvent(new Event('input', {
                        bubbles: false,
                        cancelable: true,
                    }));
                }
            }
        }
        if (!options || !options.noNotify) {
            this.valueChanges.notify(value);
            interacted = true;
        }
        if (!options || !options.noValidate) {
            if (this.parent) {
                for (let fieldProp in this.parent.fields) {
                    if ((_a = this.parent.fields[fieldProp]) === null || _a === void 0 ? void 0 : _a.touched) {
                        (_b = this.parent.fields[fieldProp]) === null || _b === void 0 ? void 0 : _b.validate();
                    }
                }
            }
            else {
                this.validate();
            }
            interacted = true;
        }
        if (interacted && (!this.parent || !this.parent.isReseting))
            this.renderIfNeed();
    }
    validate() {
        let errors = {};
        for (let validator of this.validators) {
            if (validator) {
                let error = validator(this);
                if (util_1.isObject(error) && Object.keys(error).some(x => error[x]))
                    Object.keys(error).forEach((prop) => {
                        if (error[prop])
                            errors[prop] = error[prop];
                    });
            }
        }
        this._errors = JSON.stringify(errors) === '{}' ? null : errors;
        return this.isValid;
    }
    hasError(name) {
        return !!(this.errors && this.errors[name]);
    }
    hasErrors() {
        return !!(this.errors && Object.keys(this.errors).length > 0);
    }
    setElement(element) {
        if (element) {
            if (this._elements.indexOf(element) < 0) {
                this._elements.push(element);
            }
            this.setFieldElementListeners(element);
        }
    }
    setErrors(errors) {
        this._errors = errors;
    }
    setValidators(validators) {
        if (validators) {
            this._validators = [];
            for (let validator of validators) {
                if (!this._validators.some(x => x === validator))
                    this._validators.push(validator);
            }
            ;
        }
    }
    setTouched() { this._touched = true; }
    setUntouched() { this._touched = false; }
    setBlurred() { this._blurred = true; }
    setUnblurred() { this._blurred = false; }
    disable() { this._isEnabled = false; }
    enable() { this._isEnabled = true; }
    removeValidators(validators) {
        if (validators)
            this._validators = this._validators.filter(x => !validators.some(y => x === y));
    }
    /**Reset value and clear touched and blurred properties. */
    reset(options) {
        this.setUntouched();
        this.setUnblurred();
        this.setValue(null, options);
        this._errors = null;
    }
    setFieldElementListeners(element) {
        this.listenerCollector.subscribe(element, 'focus', () => {
            this._touched = true;
            if (this.blurred)
                this.validate();
            this.renderIfNeed();
        }, true);
        this.listenerCollector.subscribe(element, 'blur', () => {
            this._blurred = true;
            this.validate();
            this.renderIfNeed();
        }, true);
    }
    renderIfNeed() {
        return __awaiter(this, void 0, void 0, function* () {
            if (this.parent && this.parent.renderOnInteract && this.parent.scope && !this.parent.isSubmitting) {
                this.parent.scope.onNeedRerender && (yield this.parent.scope.onNeedRerender(this.parent.scope));
            }
        });
    }
}
exports.FormField = FormField;
//# sourceMappingURL=form-field.js.map
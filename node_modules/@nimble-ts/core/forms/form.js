"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Form = void 0;
const form_field_1 = require("./form-field");
const app_1 = require("../app");
const listeners_collector_1 = require("../providers/listeners-collector");
const observer_1 = require("../core/observer");
class Form {
    constructor(fields) {
        this.onSubmit = new observer_1.Observer();
        this._fields = {};
        this._renderOnInteract = true;
        this._submitted = false;
        this._isSubmitting = false;
        if (fields && Object.keys(fields).length > 0) {
            for (let field in fields) {
                let formField = new form_field_1.FormField();
                formField.parent = this;
                formField.setValidators(fields[field].validators);
                formField.setValue(fields[field].value, { noNotify: true, noUpdateElement: true, noValidate: true });
                this._fields[field] = formField;
            }
        }
    }
    get fields() { return this._fields ? this._fields : {}; }
    ;
    get formElement() { return this._formElement; }
    set formElement(value) {
        this._formElement = value;
        this.setFormListeners();
    }
    get renderOnInteract() { return this._renderOnInteract; }
    set renderOnInteract(value) { this._renderOnInteract = value; }
    get scope() { return this._scope; }
    set scope(value) { this._scope = value; }
    get submitted() { return this._submitted; }
    get isSubmitting() { return this._isSubmitting; }
    get isEnabled() { return Object.keys(this._fields).every(x => this._fields[x].isEnabled); }
    get isDisabled() { return Object.keys(this._fields).every(x => this._fields[x].isDisabled); }
    /** Indicates that you have already entered at least one field and left. */
    get blurred() { return Object.keys(this._fields).some(x => this._fields[x].blurred); }
    /** Indicates that you have already entered at least one field. */
    get touched() { return Object.keys(this._fields).some(x => this._fields[x].touched); }
    get errors() {
        let errors = {};
        for (let fieldName in this._fields) {
            let field = this._fields[fieldName];
            if (field.errors) {
                let errosName = Object.keys(field.errors);
                for (let errorName of errosName) {
                    if (!(errorName in errors))
                        errors[errorName] = field.errors[errorName];
                }
            }
        }
        return JSON.stringify(errors) === '{}' ? null : errors;
    }
    /**Check if all validators from fields is valid and returns a boolean */
    get isValid() {
        let fieldsName = Object.keys(this.fields);
        return fieldsName.length === 0 || fieldsName.every(name => this.fields[name].isValid);
    }
    get listenerCollector() { return app_1.NimbleApp.inject(listeners_collector_1.ElementListenersCollector); }
    get isReseting() { return this._isReseting; }
    /**Returns form-field by name */
    get(fieldName) {
        if (fieldName in this.fields)
            return this.fields[fieldName];
        else {
            console.error(`The field "${fieldName}" not found.`);
            return new form_field_1.FormField({});
        }
    }
    get value() {
        let value = {};
        for (let field in this._fields) {
            value[field] = this.get(field).value;
        }
        return value;
    }
    has(fieldName) {
        return !!(this.fields && Object.keys(this.fields).some(name => name === fieldName));
    }
    hasErrors() {
        let errors = this.errors;
        return !!(errors && Object.keys(errors).length > 0);
    }
    /**Reset all fields value and clear touched and blurred properties. */
    reset(options) {
        try {
            this._isReseting = true;
            for (let fieldName in this.fields) {
                let field = this.fields[fieldName];
                field.reset(options);
            }
        }
        finally {
            this._isReseting = false;
            this.renderIfNeed();
            return this;
        }
    }
    /**Validates all form and returns a boolean */
    validate() {
        let isValid = true;
        for (let fieldName in this.fields) {
            let field = this.fields[fieldName];
            if (!field.validate())
                isValid = false;
        }
        return isValid;
    }
    setTouched() {
        for (let fieldName in this.fields)
            this.fields[fieldName].setTouched();
    }
    setUntouched() {
        for (let fieldName in this.fields)
            this.fields[fieldName].setUntouched();
    }
    setBlurred() {
        for (let fieldName in this.fields)
            this.fields[fieldName].setBlurred();
    }
    setUnblurred() {
        for (let fieldName in this.fields)
            this.fields[fieldName].setUnblurred();
    }
    disable() {
        for (let fieldName in this.fields)
            this.fields[fieldName].disable();
    }
    enable() {
        for (let fieldName in this.fields)
            this.fields[fieldName].enable();
    }
    setFormListeners() {
        this.listenerCollector.subscribe(this.formElement, 'submit', (e) => {
            this._isSubmitting = true;
            this._submitted = true;
            this.validate();
            this.onSubmit.notify(e);
            this.renderIfNeed();
            this._isSubmitting = false;
        }, true);
    }
    renderIfNeed() {
        return __awaiter(this, void 0, void 0, function* () {
            if (this.renderOnInteract && this.scope) {
                this.scope.onNeedRerender && (yield this.scope.onNeedRerender(this.scope));
            }
        });
    }
}
exports.Form = Form;
//# sourceMappingURL=form.js.map
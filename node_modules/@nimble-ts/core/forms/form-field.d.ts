import { Form } from "./form";
import { Observer } from "../core/observer";
export declare class FormField {
    parent: Form;
    private _elements;
    get elements(): (HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement)[];
    private _value;
    get value(): any;
    private _valueChanges;
    get valueChanges(): Observer<any>;
    private _errors;
    get errors(): {
        [name: string]: any;
    };
    private _validators;
    get validators(): ((formField: FormField) => any)[];
    private _isEnabled;
    get isEnabled(): boolean;
    get isDisabled(): boolean;
    private _blurred;
    /** Indicates that you have already entered the field and left. */
    get blurred(): boolean;
    private _touched;
    /** Indicates that you have already entered the field. */
    get touched(): boolean;
    /**Check if all validators is valid and returns a boolean */
    get isValid(): boolean;
    private get listenerCollector();
    constructor(obj?: Partial<FormField>);
    setValue(value: any, options?: {
        noNotify?: boolean;
        noUpdateElement?: boolean;
        noValidate?: boolean;
    }): void;
    validate(): boolean;
    hasError(name: string): boolean;
    hasErrors(): boolean;
    setElement(element: HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement): void;
    setErrors(errors: {
        [name: string]: boolean;
    }): void;
    setValidators(validators: ((formField: FormField) => any)[]): void;
    setTouched(): void;
    setUntouched(): void;
    setBlurred(): void;
    setUnblurred(): void;
    disable(): void;
    enable(): void;
    removeValidators(validators: ((formField: FormField) => any)[]): void;
    /**Reset value and clear touched and blurred properties. */
    reset(options?: {
        noNotify?: boolean;
        noUpdateElement?: boolean;
        noValidate?: boolean;
    }): void;
    private setFieldElementListeners;
    private renderIfNeed;
}

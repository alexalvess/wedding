import { Dialog } from "./dialog";
import { DialogRef } from "./dialog-ref";
import { ElementStructure } from "../../render/element-structure";
export declare class DialogRenderRef<T extends Dialog> {
    structuredTemplate: ElementStructure;
    dialogRef: DialogRef<T>;
    listenerCancelFunctions: (() => void)[];
    rootElement: HTMLElement;
    get dialogElement(): HTMLElement;
    get containerElement(): HTMLElement;
    get panelElement(): HTMLElement;
    get areaElement(): HTMLElement;
    constructor(init: {
        dialogRef: DialogRef<T>;
        structuredTemplate: ElementStructure;
    });
    insertDialogArea(node: Node): void;
    closeElements(onEnd: () => void): void;
    removeFromDOM(): void;
    private applyAnotherConfig;
    private applyConfigDimesions;
}

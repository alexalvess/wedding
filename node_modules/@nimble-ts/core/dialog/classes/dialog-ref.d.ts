import { DialogOpenConfig } from "./dialog-open-config";
import { Dialog } from "./dialog";
import { Type } from "../../inject/type.interface";
export declare class DialogRef<T extends Dialog> {
    private getElement;
    private builderClose;
    data: any;
    config: DialogOpenConfig;
    escClose: boolean;
    clickoutClose: boolean;
    onOpen: Promise<any>;
    onClose: Promise<any>;
    instance: T;
    type: Type<T>;
    get element(): HTMLElement;
    private promiseCloseResolve;
    constructor(obj: Partial<DialogRef<T>>, getElement: () => HTMLElement, builderClose: (ref: DialogRef<T>, resolver: (a: any) => void, data?: any) => void);
    close(data?: any): void;
}

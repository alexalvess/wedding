"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DialogRefCollector = void 0;
const injectable_1 = require("../inject/injectable");
const listeners_collector_1 = require("../providers/listeners-collector");
let DialogRefCollector = class DialogRefCollector {
    constructor(listenerCollector) {
        this.listenerCollector = listenerCollector;
        this.dialogInstances = [];
    }
    addOpenedDialog(dialog) {
        this.dialogInstances.push(dialog);
        document.body.style.overflow = 'hidden';
    }
    remove(renderRef) {
        this.removeListenersDialog(renderRef);
        let index = this.dialogInstances.indexOf(renderRef);
        delete this.dialogInstances[index];
        this.dialogInstances = this.dialogInstances.filter((x, i) => i !== index);
        if (this.dialogInstances.length === 0) {
            document.body.style.overflow = '';
        }
    }
    getDialogRefByDialogInstance(dialog) {
        let ref = this.dialogInstances.find(x => x.dialogRef.instance === dialog);
        return ref;
    }
    getLast() {
        let length = this.dialogInstances.length;
        return length > 0 ? this.dialogInstances[length - 1] : null;
    }
    removeListenersDialog(renderRef) {
        this.removeStructuredListeners(renderRef.structuredTemplate);
    }
    removeStructuredListeners(structured) {
        if (structured.compiledNode) {
            this.listenerCollector.unsubscribeAllFromElement(structured.compiledNode);
        }
        if (structured.children && structured.children.length > 0) {
            for (let child of structured.children) {
                this.removeStructuredListeners(child);
            }
        }
    }
};
DialogRefCollector = __decorate([
    injectable_1.Injectable({ single: true }),
    __metadata("design:paramtypes", [listeners_collector_1.ElementListenersCollector])
], DialogRefCollector);
exports.DialogRefCollector = DialogRefCollector;
//# sourceMappingURL=dialog-ref-collector.js.map
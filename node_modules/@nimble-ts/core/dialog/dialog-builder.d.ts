import { InjectionToken } from '../inject/provider';
import { Type } from '../inject/type.interface';
import { DialogRef } from './classes/dialog-ref';
import { Dialog } from './classes/dialog';
import { DialogOpenConfig } from './classes/dialog-open-config';
import { DialogRender } from './dialog-render';
import { DialogRefCollector } from './dialog-ref-collector';
export declare const DIALOG_REF: InjectionToken;
export declare class DialogBuilder {
    private dialogRender;
    private dialogCollector;
    private get app();
    constructor(dialogRender: DialogRender, dialogCollector: DialogRefCollector);
    open<T extends Dialog>(dialog: Type<T>, config?: DialogOpenConfig): DialogRef<T>;
    closeAll(): void;
    private close;
    private rerender;
}

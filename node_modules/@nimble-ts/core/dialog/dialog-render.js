"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DialogRender = void 0;
const injectable_1 = require("../inject/injectable");
const dialog_render_ref_1 = require("./classes/dialog-render-ref");
const render_abstract_1 = require("../render/render-abstract");
const listener_1 = require("../render/listener");
const listeners_collector_1 = require("../providers/listeners-collector");
const render_helper_1 = require("../render/render-helper");
const dialog_ref_collector_1 = require("./dialog-ref-collector");
let DialogRender = class DialogRender extends render_abstract_1.RenderAbstract {
    constructor(listenersCollector, listener, dialogRefCollector) {
        super(listenersCollector);
        this.listenersCollector = listenersCollector;
        this.listener = listener;
        this.dialogRefCollector = dialogRefCollector;
    }
    renderDialog(dialogRef) {
        let renderRef = new dialog_render_ref_1.DialogRenderRef({
            dialogRef: dialogRef,
            structuredTemplate: this.createStructure(dialogRef)
        });
        this.createElementFromStructure(renderRef.structuredTemplate);
        let templateCompiled = this.compileElementFromStructure(renderRef.structuredTemplate);
        this.rerenderDialog(renderRef);
        renderRef.insertDialogArea(templateCompiled);
        this.applyCloseEvents(renderRef);
        return renderRef;
    }
    rerenderDialog(renderRef) {
        if (renderRef) {
            this.recompileElementFromStructure(renderRef.structuredTemplate);
        }
    }
    createStructure(dialog) {
        let htmlTemplate = dialog.instance.template;
        let scope = dialog.instance;
        return render_helper_1.RenderHelper.buildStructureFromTemplate(htmlTemplate, scope, 'nimble-dialog-area');
    }
    applyCloseEvents(renderRef) {
        this.applyClickOutEvent(renderRef);
        this.applyEscapeEvent(renderRef);
    }
    applyClickOutEvent(renderRef) {
        let areaElement = renderRef.areaElement;
        let backdropElement = renderRef.rootElement.querySelector('.nimble-dialog-backdrop');
        let containerElement = renderRef.containerElement;
        let canClose = false;
        renderRef.listenerCancelFunctions.push(this.listener.listen(renderRef.rootElement, 'mousedown', (e) => {
            const lastDialogRenderRef = this.dialogRefCollector.getLast();
            if (!canClose && (renderRef === null || renderRef === void 0 ? void 0 : renderRef.dialogRef) === lastDialogRenderRef.dialogRef && (backdropElement === e.target || containerElement === e.target)) {
                let bounds = areaElement.getBoundingClientRect();
                if (e.pageX < bounds.left || e.pageX > bounds.right || e.pageY < bounds.top || e.pageY > bounds.bottom) {
                    if (renderRef.dialogRef.clickoutClose) {
                        canClose = true;
                    }
                }
            }
        }));
        renderRef.listenerCancelFunctions.push(this.listener.listen(renderRef.rootElement, 'mouseup', (e) => {
            if (canClose)
                renderRef.dialogRef.close();
        }));
    }
    applyEscapeEvent(renderRef) {
        renderRef.listenerCancelFunctions.push(this.listener.listen(window, 'keydown', (e) => {
            const lastDialogRenderRef = this.dialogRefCollector.getLast();
            if ((renderRef === null || renderRef === void 0 ? void 0 : renderRef.dialogRef) === lastDialogRenderRef.dialogRef) {
                if (e.keyCode === 27 && renderRef.dialogRef.escClose)
                    renderRef.dialogRef.close();
            }
        }));
    }
};
DialogRender = __decorate([
    injectable_1.Injectable({ single: true }),
    __metadata("design:paramtypes", [listeners_collector_1.ElementListenersCollector,
        listener_1.ElementListener,
        dialog_ref_collector_1.DialogRefCollector])
], DialogRender);
exports.DialogRender = DialogRender;
//# sourceMappingURL=dialog-render.js.map
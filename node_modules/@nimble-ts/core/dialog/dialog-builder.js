"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DialogBuilder = exports.DIALOG_REF = void 0;
const injectable_1 = require("../inject/injectable");
const provider_1 = require("../inject/provider");
const dialog_ref_1 = require("./classes/dialog-ref");
const app_1 = require("../app");
const dialog_render_1 = require("./dialog-render");
const dialog_ref_collector_1 = require("./dialog-ref-collector");
exports.DIALOG_REF = new provider_1.InjectionToken('DIALOG_REF');
let DialogBuilder = class DialogBuilder {
    constructor(dialogRender, dialogCollector) {
        this.dialogRender = dialogRender;
        this.dialogCollector = dialogCollector;
    }
    get app() { return app_1.NimbleApp.instance; }
    open(dialog, config) {
        let dialogRenderRef;
        let openResolve;
        const dialogRef = new dialog_ref_1.DialogRef({
            config: config,
            type: dialog,
            onOpen: new Promise((resolve) => {
                openResolve = resolve;
            })
        }, () => dialogRenderRef.rootElement, this.close.bind(this));
        app_1.NimbleApp.registerProvider({ provide: exports.DIALOG_REF, useValue: dialogRef });
        dialogRef.instance = app_1.NimbleApp.inject(dialog);
        dialogRef.instance.onNeedRerender = this.rerender.bind(this);
        dialogRenderRef = this.dialogRender.renderDialog(dialogRef);
        this.dialogCollector.addOpenedDialog(dialogRenderRef);
        openResolve();
        dialogRef.instance.onOpen();
        app_1.NimbleApp.unregisterProvider(exports.DIALOG_REF);
        return dialogRef;
    }
    closeAll() {
        let dialogRenderRef = this.dialogCollector.getLast();
        while (dialogRenderRef) {
            dialogRenderRef.dialogRef.close();
            dialogRenderRef = this.dialogCollector.getLast();
        }
    }
    close(ref, resolver, data) {
        let dialogRenderRef = (ref instanceof dialog_ref_1.DialogRef) ? this.dialogCollector.getDialogRefByDialogInstance(ref.instance) : ref;
        if (dialogRenderRef) {
            dialogRenderRef.closeElements(() => {
                dialogRenderRef.removeFromDOM();
                dialogRenderRef.dialogRef.instance.onClose();
                resolver(data);
            });
            this.dialogCollector.remove(dialogRenderRef);
        }
    }
    rerender(dilaog) {
        this.app.state = app_1.NimbleAppState.RERENDERING;
        let dialogRenderRef = this.dialogCollector.getDialogRefByDialogInstance(dilaog);
        this.dialogRender.rerenderDialog(dialogRenderRef);
        this.app.state = app_1.NimbleAppState.INITIALIZED;
    }
};
DialogBuilder = __decorate([
    injectable_1.Injectable(),
    __metadata("design:paramtypes", [dialog_render_1.DialogRender,
        dialog_ref_collector_1.DialogRefCollector])
], DialogBuilder);
exports.DialogBuilder = DialogBuilder;
//# sourceMappingURL=dialog-builder.js.map
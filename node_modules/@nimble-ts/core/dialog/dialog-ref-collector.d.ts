import { Dialog } from "./classes/dialog";
import { DialogRenderRef } from "./classes/dialog-render-ref";
import { ElementListenersCollector } from "../providers/listeners-collector";
export declare class DialogRefCollector {
    private listenerCollector;
    constructor(listenerCollector: ElementListenersCollector);
    private dialogInstances;
    addOpenedDialog(dialog: DialogRenderRef<Dialog>): void;
    remove(renderRef: DialogRenderRef<Dialog>): void;
    getDialogRefByDialogInstance(dialog: Dialog): DialogRenderRef<Dialog>;
    getLast(): DialogRenderRef<Dialog>;
    private removeListenersDialog;
    private removeStructuredListeners;
}

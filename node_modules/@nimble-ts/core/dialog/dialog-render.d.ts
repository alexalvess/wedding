import { Dialog } from "./classes/dialog";
import { DialogRenderRef } from "./classes/dialog-render-ref";
import { DialogRef } from "./classes/dialog-ref";
import { RenderAbstract } from "../render/render-abstract";
import { ElementListener } from "../render/listener";
import { ElementListenersCollector } from "../providers/listeners-collector";
import { DialogRefCollector } from "./dialog-ref-collector";
export declare class DialogRender extends RenderAbstract {
    protected listenersCollector: ElementListenersCollector;
    private listener;
    private dialogRefCollector;
    constructor(listenersCollector: ElementListenersCollector, listener: ElementListener, dialogRefCollector: DialogRefCollector);
    renderDialog<T extends Dialog>(dialogRef: DialogRef<T>): DialogRenderRef<T>;
    rerenderDialog<T extends Dialog>(renderRef: DialogRenderRef<T>): void;
    private createStructure;
    private applyCloseEvents;
    private applyClickOutEvent;
    private applyEscapeEvent;
}

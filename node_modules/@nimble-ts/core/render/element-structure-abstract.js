"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AttributeStructure = exports.ElementStructureAbstract = void 0;
const util_1 = require("util");
const render_helper_1 = require("./render-helper");
const base_form_field_directive_1 = require("../directives/abstracts/base-form-field-directive");
const form_directive_1 = require("../directives/forms/form.directive");
const directive_helper_1 = require("../directives/directive.helper");
const app_1 = require("../app");
class ElementStructureAbstract {
    constructor(scope) {
        this.parent = null;
        this.tagName = '';
        this.content = '';
        this.children = [];
        this.directivesInstance = [];
        this.rawNode = null;
        this.compiledNode = null;
        this.compiledBeginFn = null;
        this.compiledEndFn = null;
        this.attrs = [];
        this.attrDirectives = { default: [], iterate: { directive: null, props: { in: [], out: [] } } };
        this.isVoid = false;
        this.isPureElement = false;
        this.isRendered = false;
        this.hasInterpolationInText = false;
        this.scope = scope;
    }
    get hasParent() { return !util_1.isNullOrUndefined(this.parent); }
    get hasChildren() { return this.children.length > 0; }
    get hasAttritubes() { return this.attrs.length > 0; }
    get isText() { return this.tagName ? false : true; }
    get hasNormalDirectivesToApply() { return this.attrDirectives.default.length > 0; }
    get hasIterationDirectivesToApply() { return !!this.attrDirectives.iterate.directive; }
    get nodeIsRenderedInDOM() { return this.compiledNode && !util_1.isNullOrUndefined(this.compiledNode.parentNode); }
    removeCompiledNode(onRemoveNode) {
        if (this.hasChildren) {
            for (const child of this.children) {
                child.removeCompiledNode(onRemoveNode);
            }
        }
        if (this.nodeIsRenderedInDOM) {
            this.compiledNode.parentNode.removeChild(this.compiledNode);
            this.isRendered = false;
        }
        this.destroyAllDirectives();
        if (onRemoveNode)
            onRemoveNode(this);
    }
    destroyAllDirectives() {
        for (const attr of this.attrDirectives.default) {
            attr.directive.destroyDirective();
        }
        if (this.attrDirectives.iterate.directive) {
            this.attrDirectives.iterate.directive.destroyDirective();
        }
    }
    getIterationDirective() {
        const attr = this.attrDirectives.iterate.directive;
        const props = this.attrDirectives.iterate.props;
        if (attr) {
            let instance = this.directivesInstance.find(x => x.selector === attr.name);
            if (!instance) {
                instance = app_1.NimbleApp.inject(attr.directiveType);
                instance.scope = this.scope;
                instance.element = this.compiledNode;
                instance.selectorActive = attr.name;
                instance.insertInput(instance.selector, () => attr.value);
                props.in.forEach((attr) => {
                    instance.insertInput(attr.name, () => AttributeStructure.getCompiledValue(attr.name, attr.value, this.scope));
                });
                props.out.forEach((attr) => {
                    instance.insertOutput(attr.name.replace(/\(|\)/g, ''), () => attr.value);
                });
                attr.directiveInstance = instance;
                this.directivesInstance.push(instance);
            }
            else {
                instance.element = this.compiledNode;
            }
            instance.all = () => this.directivesInstance;
        }
        return attr;
    }
    resolveAttrs() {
        for (const attr of this.attrs.filter(x => x.hasInterpolationInText || !x.isResolved)) {
            render_helper_1.RenderHelper.resolveDefaultAttribute(this.compiledNode, attr.name, attr.value, this.scope);
            attr.isResolved = true;
        }
    }
    resolveAttrDirectivesIfNeeded() {
        for (const attr of this.attrDirectives.default) {
            attr.directive.resolveDirectiveIfNeeded();
        }
    }
    getIterationStructuresFromSelf() {
        return this.parent.children.filter(x => x.from === this);
    }
    instantiateAttrDirectives() {
        const attrsDefaults = this.attrDirectives.default.filter(x => !x.directive.isResolved);
        for (const attrDefault of attrsDefaults) {
            const attr = attrDefault.directive;
            const props = attrDefault.props;
            let instance = this.directivesInstance.find(x => x.selector === attr.name.replace(/\[|\]/g, ''));
            if (!instance) {
                instance = app_1.NimbleApp.inject(attr.directiveType);
                instance.element = this.compiledNode;
                instance.scope = this.scope;
                instance.selectorActive = attr.name;
                const inputs = props.in.map(x => ({
                    name: x.name,
                    value: () => AttributeStructure.getCompiledValue(attr.name, attr.value, this.scope),
                }));
                const outputs = props.out.map(x => ({
                    name: x.name.replace(/\(|\)/g, ''),
                    value: () => x.value,
                }));
                if (instance.selectorIsInput) {
                    inputs.push({
                        name: instance.selector,
                        value: () => AttributeStructure.getCompiledValue(attr.name, attr.value, this.scope)
                    });
                }
                else {
                    outputs.push({ name: instance.selector, value: () => attr.value });
                }
                inputs.forEach((x) => instance.insertInput(x.name, x.value));
                outputs.forEach((x) => instance.insertOutput(x.name, x.value));
                if (attr.directiveInstance) {
                    attr.directiveInstance.all = () => this.directivesInstance;
                }
                attr.directiveInstance = instance;
                this.directivesInstance.push(instance);
            }
        }
    }
    renderNodeIfNot() {
        this.checkRenderedCorrectly();
        if (!this.isRendered && this.parent && this.parent.compiledNode) {
            const structureIndex = this.parent.children.findIndex(x => x === this);
            const getNextNodeAbove = () => {
                for (let i = structureIndex - 1; i >= 0; i--) {
                    const node = this.parent.children[i];
                    if (node.compiledNode && node.compiledNode.parentNode === this.parent.compiledNode && node.nodeIsRenderedInDOM) {
                        return node.compiledNode.nextSibling;
                    }
                }
                return null;
            };
            const getNextNodeBelow = () => {
                for (let i = structureIndex + 1; i < this.parent.children.length; i++) {
                    const node = this.parent.children[i];
                    if (node.compiledNode && node.compiledNode.parentNode === this.parent.compiledNode && node.nodeIsRenderedInDOM) {
                        return node.compiledNode;
                    }
                }
                return null;
            };
            const nodeAbove = getNextNodeAbove();
            if (nodeAbove) {
                this.parent.compiledNode.insertBefore(this.compiledNode, nodeAbove);
                this.isRendered = true;
            }
            else {
                const nodeBelow = getNextNodeBelow();
                if (nodeBelow) {
                    this.parent.compiledNode.insertBefore(this.compiledNode, nodeBelow);
                    this.isRendered = true;
                }
                else {
                    this.parent.compiledNode.appendChild(this.compiledNode);
                    this.isRendered = true;
                }
            }
        }
    }
    checkRenderedCorrectly() {
        if (this.nodeIsRenderedInDOM) {
            if (this.parent && this.compiledNode.parentNode !== this.parent.compiledNode) {
                this.compiledNode.parentNode.removeChild(this.compiledNode);
            }
        }
    }
}
exports.ElementStructureAbstract = ElementStructureAbstract;
class AttributeStructure {
    constructor(name, value, strucutre, directiveType = null, fromClone = false) {
        this.isResolved = false;
        this.hasInterpolationInText = false;
        this.structure = null;
        this.name = '';
        this.value = '';
        this.directiveType = null;
        this.directiveInstance = null;
        this.name = name;
        this.value = value;
        this.structure = strucutre;
        this.directiveType = directiveType !== null && directiveType !== void 0 ? directiveType : (!fromClone ? this.getDirective() : null);
    }
    get allDirectives() { return app_1.NimbleApp.instance.directives; }
    get isIterationDirective() { return this.directiveType != null && this.directiveType.prototype.type === 'IterationDirective'; }
    get isDefaultDirective() { return this.directiveType != null && this.directiveType.prototype.type !== 'IterationDirective'; }
    get isNotDirective() { return !this.directiveType; }
    resolveDirectiveIfNeeded() {
        if (!this.isResolved) {
            this.isResolved = true;
            this.checkDirectiveBeforeResolve();
            this.directiveInstance.onRender();
        }
        else {
            this.checkDirectiveBeforeResolve();
            this.directiveInstance.onChange();
        }
    }
    destroyDirective() {
        if (this.directiveInstance) {
            this.directiveInstance.onDestroy();
        }
        this.isResolved = false;
    }
    checkDirectiveBeforeResolve() {
        if (this.directiveInstance instanceof base_form_field_directive_1.BaseFormFieldDirective) {
            let structure = this.structure.parent;
            let formDirective = null;
            while (!formDirective && structure) {
                formDirective = structure.directivesInstance.find(x => x instanceof form_directive_1.FormDirective);
                structure = structure.parent;
            }
            this.directiveInstance.form = formDirective && formDirective.form;
        }
    }
    getDirective() {
        let directive = null;
        const attrName = this.name.toLowerCase();
        for (let directiveType of this.allDirectives) {
            const selectors = directiveType.prototype.selectors;
            const selector = selectors.find(selector => {
                if (selector) {
                    selector = selector.toLowerCase();
                    if (/^\[([^)]+)\]$/g.test(attrName) && /^(?!\(\/).*(?!\))$/g.test(selector))
                        return attrName === `[${selector.replace(/\[|\]/g, '')}]`;
                    else
                        return attrName === selector;
                }
                return false;
            });
            if (selector) {
                directive = directiveType;
                break;
            }
        }
        if (!/^\(([^)]+)\)$/g.test(attrName)) {
            if (!/^\[([^)]+)\]$/g.test(attrName) && directive_helper_1.DirectiveHelper.checkSelectorMustHavePureValue(attrName)) {
                return null;
            }
        }
        return directive;
    }
    static getCompiledValue(propName, propValue, scope) {
        if (!/^\(([^)]+)\)$/g.test(propName)) {
            if (/^\[([^)]+)\]$/g.test(propName)) {
                if (!directive_helper_1.DirectiveHelper.checkSelectorMustHavePureValue(propName)) {
                    propValue = scope.compile(propValue);
                }
            }
            else {
                propValue = render_helper_1.RenderHelper.resolveInterpolationIfHave(propValue, scope);
            }
        }
        return propValue;
    }
}
exports.AttributeStructure = AttributeStructure;
//# sourceMappingURL=element-structure-abstract.js.map
"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.HeaderRender = void 0;
const app_1 = require("./../app");
const util_1 = require("util");
const injectable_1 = require("../inject/injectable");
let HeaderRender = class HeaderRender {
    get app() { return app_1.NimbleApp.instance; }
    constructor() {
    }
    resolveTitleAndMetaTags(route) {
        var _a;
        let page = route.pageInstance;
        let titleElement = this.resolveTitle(page.title);
        if (page.meta) {
            let element = (_a = this.resolveMetaAndContent('name="description"', page.meta.description, titleElement)) !== null && _a !== void 0 ? _a : titleElement;
            element = this.resolveMetaAndContent('name="keywords"', page.meta.keywords, element);
            if (page.meta.og) {
                let og = page.meta.og;
                let ogProps = Object.keys(page.meta.og)
                    .filter(x => !util_1.isNullOrUndefined(og[x]) && og[x] !== '')
                    .map(x => x.replace(/([A-Z])/g, (v) => `_${v.toLowerCase()}`));
                if (page.title) {
                    for (let prop of ['title', 'siteName'].filter(prop => !ogProps.some(p => p === prop))) {
                        ogProps.push(prop);
                        og[prop] = page.title;
                    }
                }
                if (!ogProps.some(p => p === 'description') && page.meta.description) {
                    ogProps.push('description');
                    og['description'] = page.meta.description;
                }
                for (let property of ogProps) {
                    element = this.resolveMetaAndContent(`property="og:${property}"`, og[property], element);
                }
            }
        }
    }
    resolveTitle(title) {
        let titleEl = document.head.querySelector('title');
        if (titleEl) {
            titleEl.textContent = title !== null && title !== void 0 ? title : '';
        }
        else {
            titleEl = document.createElement('title');
            titleEl.textContent = title !== null && title !== void 0 ? title : '';
            document.head.prepend(titleEl);
        }
        return titleEl;
    }
    resolveMetaAndContent(metaSelector, content, afterElement) {
        if (metaSelector && content) {
            let metaEl = document.head.querySelector(`[${metaSelector}]`);
            if (!metaEl) {
                metaEl = document.createElement('meta');
                let property = metaSelector.split('=')[0];
                let value = metaSelector.split('=')[1].replace(/"/g, '');
                if (afterElement) {
                    afterElement.after(metaEl);
                }
                else {
                    document.head.prepend(metaEl);
                }
                metaEl.setAttribute(property, value);
            }
            metaEl.setAttribute('content', content);
            return metaEl;
        }
        return afterElement;
    }
};
HeaderRender = __decorate([
    injectable_1.Injectable({ single: true }),
    __metadata("design:paramtypes", [])
], HeaderRender);
exports.HeaderRender = HeaderRender;
//# sourceMappingURL=header-render.js.map
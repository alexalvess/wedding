"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.NimbleAppState = exports.NimbleApp = void 0;
require("@webcomponents/webcomponentsjs/custom-elements-es5-adapter.js");
const router_1 = require("./route/router");
const nimble_page_element_1 = require("./elements/nimble-page.element");
const nimble_router_element_1 = require("./elements/nimble-router.element");
const nimble_dialog_element_1 = require("./elements/nimble-dialog.element");
const nimble_dialog_area_element_1 = require("./elements/nimble-dialog-area.element");
const router_state_enum_1 = require("./route/router-state.enum");
const internal_directives_1 = require("./directives/internal-directives");
const container_1 = require("./inject/container");
const internal_providers_1 = require("./providers/internal-providers");
const listener_1 = require("./render/listener");
const route_render_1 = require("./route/route-render");
class NimbleApp {
    constructor(config) {
        this.config = config;
        this.containerInjector = new container_1.Container();
        this.afterRenderFn = null;
        this.state = NimbleAppState.INITIALIZING;
        this.firstRender = true;
        this.changedRoute = false;
        this._rootElement = null;
        this._baseHref = '/';
        this.iterationElementsApplied = {
            real: [],
            virtual: []
        };
        this.directives = [];
    }
    get rootElement() { return this._rootElement; }
    get baseHref() { return this._baseHref; }
    get hasBaseHref() { return this.baseHref !== '' && this.baseHref !== '/'; }
    get routes() { return router_1.Router.routes; }
    static config(config) {
        if (!this.instance) {
            this.instance = new NimbleApp(config);
            this.instance.afterInstanciate();
        }
        return this.instance;
    }
    afterInstanciate() {
        var _a, _b;
        try {
            this.defineRootElement();
            this.registerDirectives();
            this.registerProvidersInContainerInjector();
            this._baseHref = (_b = (_a = document.head.querySelector('base')) === null || _a === void 0 ? void 0 : _a.href) !== null && _b !== void 0 ? _b : '/';
            this._baseHref = this._baseHref.replace(location.origin, '');
            // Router
            router_1.Router.useHash = this.config.useHash;
            router_1.Router.registerRoutes(this.config.routes);
            this.routeRender = this.containerInjector.inject(route_render_1.RouteRender);
        }
        catch (e) {
            console.error(e);
        }
    }
    defineRootElement() {
        this._rootElement = document.querySelector('nimble-root');
        if (this.rootElement) {
            this.rootElement.innerHTML = '';
            this.rootElement.style.removeProperty('visibility');
        }
        else {
            console.error(`Nimble not work, because the '<nimble-root></nimble-root>' element not found in body.`);
        }
    }
    registerDirectives() {
        let directives = [...internal_directives_1.INTERNAL_DIRECTIVES];
        if (this.config.directives && this.config.directives.length > 0)
            directives = directives.concat(this.config.directives);
        directives.forEach((directive) => {
            try {
                directive.prototype.validate(this.directives);
                this.directives.push(directive);
            }
            catch (e) {
                console.error(e);
            }
        });
    }
    registerProvidersInContainerInjector() {
        let providers = [...internal_providers_1.INTERNAL_PROVIDERS];
        if (this.config.providers && this.config.providers.length > 0)
            providers = providers.concat(this.config.providers);
        this.providers = providers;
        this.providers.forEach(service => {
            if (service.prototype.single)
                this.containerInjector.addProvider({ provide: service, useSingleton: service });
            else
                this.containerInjector.addProvider({ provide: service, useClass: service });
        });
    }
    start() {
        if (this.state === NimbleAppState.INITIALIZING) {
            this.registerElements();
            router_1.Router.addListener(router_state_enum_1.RouterState.STARTED_CHANGE, this.onRouteStartChange.bind(this));
            router_1.Router.addListener(router_state_enum_1.RouterState.FINISHED_CHANGE, this.onRouteFinishedChange.bind(this));
            router_1.Router.addListener(router_state_enum_1.RouterState.CHANGE_ERROR, this.onRouteChangeError.bind(this));
            router_1.Router.addListener(router_state_enum_1.RouterState.STARTED_LOADING, this.onRouteStartedLoading.bind(this));
            router_1.Router.addListener(router_state_enum_1.RouterState.FINISHED_LOADING, this.onRouteFinishedLoading.bind(this));
            router_1.Router.addListener(router_state_enum_1.RouterState.ERROR_LOADING, this.onRouteErrorLoading.bind(this));
            router_1.Router.addListener(router_state_enum_1.RouterState.STARTED_RERENDER, this.onRouteStartRerender.bind(this));
            router_1.Router.addListener(router_state_enum_1.RouterState.FINISHED_RERENDER, this.onRouteFinishedRerender.bind(this));
            router_1.Router.addListener(router_state_enum_1.RouterState.RENDERING, this.onRouteRendering.bind(this));
            router_1.Router.start();
            this.state = NimbleAppState.INITIALIZED;
        }
        return NimbleApp.instance;
    }
    registerElements() {
        window.customElements.define('nimble-page', nimble_page_element_1.NimblePage);
        window.customElements.define('nimble-router', nimble_router_element_1.NimbleRouter);
        window.customElements.define('nimble-dialog', nimble_dialog_element_1.NimbleDialog);
        window.customElements.define('nimble-dialog-area', nimble_dialog_area_element_1.NimbleDialogArea);
    }
    onRouteStartChange(event) {
        this.changedRoute = false;
    }
    onRouteRendering(event) {
        return __awaiter(this, void 0, void 0, function* () {
            if (event.route) {
                // if (Router.rerenderedBeforeFinishedRouteChange) {}
                yield this.routeRender.prepareRouteToCompileAndRender(event.route);
                this.routeRender.compileAndRenderRoute(event.route);
            }
        });
    }
    onRouteFinishedChange(event) {
        var _a;
        return __awaiter(this, void 0, void 0, function* () {
            if (event.route) {
                this.state = NimbleAppState.RERENDERING;
                const init = performance.now();
                // console.log('WILL-CHANGED', event.route.path);
                yield this.routeRender.notifyRoutesAfterRouteChanged(event.route);
                // console.log('CHANGED', event.route.path, performance.now() - init);
                this.changedRoute = true;
                (_a = event.route.pageInstance) === null || _a === void 0 ? void 0 : _a.render();
            }
        });
    }
    onRouteChangeError(event) { }
    onRouteStartedLoading(route) { }
    onRouteFinishedLoading(event) { }
    onRouteErrorLoading(event) { }
    onRouteStartRerender(event) {
        const routeState = router_1.Router.state;
        const routePrevState = router_1.Router.previousState;
        this.state = NimbleAppState.RERENDERING;
        this.routeRender.rerenderRoute(event.route);
    }
    onRouteFinishedRerender(event) {
        return __awaiter(this, void 0, void 0, function* () {
            const { isRendered } = yield this.routeRender.notifyRoutesAfterRerender(event.route);
            if (this.changedRoute && isRendered) {
                yield this.routeRender.notifyRoutesAfterInit(event.route);
                yield this.checkHashLinkAfterRouteChanged();
            }
            document.dispatchEvent(new Event('render-event'));
            this.state = NimbleAppState.INITIALIZED;
        });
    }
    checkHashLinkAfterRouteChanged() {
        return __awaiter(this, void 0, void 0, function* () {
            this.changedRoute = false;
            if (location.hash && !this.config.useHash) {
                yield new Promise((resolve) => {
                    setTimeout(() => {
                        let currentHash = location.hash;
                        location.hash = '';
                        location.hash = currentHash;
                        resolve();
                    }, 250);
                });
            }
            else {
                document.body.style.scrollBehavior = 'initial';
                document.children.item(0).style.scrollBehavior = 'initial';
                yield new Promise((resolve) => {
                    setTimeout(() => {
                        document.body.scrollTo({ top: 0, left: 0 });
                        document.children.item(0).scrollTo({ top: 0, left: 0 });
                        document.body.style.scrollBehavior = '';
                        document.children.item(0).style.scrollBehavior = '';
                        resolve();
                    });
                });
            }
        });
    }
    static inject(type, onInstaciate) {
        return this.instance.containerInjector.inject(type, onInstaciate);
    }
    static registerProvider(provider) {
        this.instance.containerInjector.addProvider(provider);
    }
    static unregisterProvider(provide) {
        this.instance.containerInjector.removeProvider(provide);
    }
    static get listener() { return this.instance.containerInjector.inject(listener_1.ElementListener); }
}
exports.NimbleApp = NimbleApp;
var NimbleAppState;
(function (NimbleAppState) {
    NimbleAppState["INITIALIZING"] = "INITIALIZING";
    NimbleAppState["INITIALIZED"] = "INITIALIZED";
    NimbleAppState["RERENDERING"] = "RERENDERING";
})(NimbleAppState = exports.NimbleAppState || (exports.NimbleAppState = {}));
//# sourceMappingURL=app.js.map
import { ObserverListener } from "../core/observer";
import { IScope } from "../page/interfaces/scope.interface";
export declare class InternalObserversCollector {
    private subscribedEvents;
    add(scope: IScope, selector: string, subscription: ObserverListener<any>): void;
    cancelAll(): void;
    cancelByScope(scope: IScope): void;
    cancelByScopeAndSelector(scope: IScope, selector: string): void;
}

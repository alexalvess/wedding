import { HttpOptions } from './http-options';
import { HttpResponse } from './http-response';
export declare class HttpClient {
    get<T>(url: string, options?: HttpOptions): Promise<HttpResponse<T>>;
    post<T>(url: string, body: any, options?: HttpOptions): Promise<HttpResponse<T>>;
    put<T>(url: string, body: any, options?: HttpOptions): Promise<HttpResponse<T>>;
    patch<T>(url: string, body: any, options?: HttpOptions): Promise<HttpResponse<T>>;
    del<T>(url: string, options?: HttpOptions): Promise<HttpResponse<T>>;
    private prepreAndGetXHR;
    private setHeaders;
    private getUrlWithParams;
}

"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.HttpClient = void 0;
const injectable_1 = require("../../inject/injectable");
const http_options_1 = require("./http-options");
const http_response_1 = require("./http-response");
const util_1 = require("util");
const http_error_1 = require("./http-error");
let HttpClient = class HttpClient {
    get(url, options) {
        return new Promise((resolve, reject) => {
            this.prepreAndGetXHR('GET', url, null, options).then((response) => {
                resolve(response);
            }, (error) => {
                reject(error);
            });
        });
    }
    post(url, body, options) {
        return new Promise((resolve, reject) => {
            this.prepreAndGetXHR('POST', url, body, options).then((response) => {
                resolve(response);
            }, (error) => {
                reject(error);
            });
        });
    }
    put(url, body, options) {
        return new Promise((resolve, reject) => {
            this.prepreAndGetXHR('PUT', url, body, options).then((response) => {
                resolve(response);
            }, (error) => {
                reject(error);
            });
        });
    }
    patch(url, body, options) {
        return new Promise((resolve, reject) => {
            this.prepreAndGetXHR('PATCH', url, body, options).then((response) => {
                resolve(response);
            }, (error) => {
                reject(error);
            });
        });
    }
    del(url, options) {
        return new Promise((resolve, reject) => {
            this.prepreAndGetXHR('DELETE', url, null, options).then((response) => {
                resolve(response);
            }, (error) => {
                reject(error);
            });
        });
    }
    prepreAndGetXHR(method, url, body, options) {
        return new Promise((resolve, reject) => {
            let client = new XMLHttpRequest();
            let urlWithParams = options ? this.getUrlWithParams(url, options.params) : url;
            if (!options)
                options = new http_options_1.HttpOptions();
            client.withCredentials = options.withCredentials;
            client.responseType = (options.responseType) ? options.responseType : 'json';
            client.open(method, urlWithParams, true);
            if (this.setHeaders(client, options.headers).isJson && util_1.isObject(body))
                body = JSON.stringify(body);
            client.onreadystatechange = function () {
                if (client.readyState == 4) {
                    let response = {
                        xhr: client,
                        status: client.status
                    };
                    if (client.status == 200) {
                        response.data = client.response;
                        let object = new http_response_1.HttpResponse(response);
                        resolve(object);
                    }
                    else {
                        response.error = client.response;
                        let object = new http_response_1.HttpResponse(response);
                        reject(object);
                    }
                }
            };
            client.onerror = function () {
                reject(new http_error_1.HttpError({
                    xhr: client,
                    status: client.status,
                    error: client.response
                }));
            };
            client.send(body);
        });
    }
    setHeaders(client, headers) {
        let response = { isJson: false };
        if (headers) {
            if (util_1.isArray(headers)) {
                for (let key of headers)
                    client.setRequestHeader(key, '');
            }
            else {
                for (let key in headers) {
                    let value = headers[key];
                    if (key.toLowerCase() === 'content-type' && value.includes('application/json'))
                        response.isJson = true;
                    client.setRequestHeader(key, value);
                }
            }
        }
        if (!headers || !util_1.isArray(headers) || !(Object.keys(headers).some(key => key.toLowerCase() === 'content-type'))) {
            client.setRequestHeader('Content-Type', 'application/json');
            response.isJson = true;
        }
        return response;
    }
    getUrlWithParams(url, params) {
        if (params && url) {
            for (let paramName in params) {
                let value = params[paramName];
                if (!url.includes('?'))
                    url += `?${paramName}=${value}`;
                else
                    url += `&${paramName}=${value}`;
            }
        }
        return url;
    }
};
HttpClient = __decorate([
    injectable_1.Injectable()
], HttpClient);
exports.HttpClient = HttpClient;
//# sourceMappingURL=http-client.js.map
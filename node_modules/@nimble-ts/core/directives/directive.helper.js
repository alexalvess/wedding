"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DirectiveHelper = void 0;
const natives_attr_directive_1 = require("./natives-attr.directive");
class DirectiveHelper {
    static getAllPureSelectors() {
        return [
            ...natives_attr_directive_1.NativesAttrsDirective.selectorsMustHavePureValue,
        ].map(x => x.replace(/\[|\(|\]|\)/g, ''));
    }
    static checkSelectorMustHavePureValue(selector) {
        selector = selector.replace(/\[|\(|\]|\)/g, '');
        return this.getAllPureSelectors().indexOf(selector) >= 0;
    }
    static isNativeSelector(selector) {
        selector = selector.replace(/\[|\(|\]|\)/g, '');
        return this.getNativesSelector().some(x => x.replace(/\[|\(|\]|\)/g, '') === selector);
    }
    static getNativesSelector() {
        return ['href', ...natives_attr_directive_1.NATIVE_SELECTORS];
    }
}
exports.DirectiveHelper = DirectiveHelper;
//# sourceMappingURL=directive.helper.js.map
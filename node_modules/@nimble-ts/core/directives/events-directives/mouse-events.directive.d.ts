import { Directive } from '../abstracts/directive';
import { ElementListener } from '../../render/listener';
export declare class MouseEventsDirective extends Directive {
    private listener;
    constructor(listener: ElementListener);
    onRender(): void;
    onChange(): void;
    onDestroy(): void;
}

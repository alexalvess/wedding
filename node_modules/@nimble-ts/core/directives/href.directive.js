"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.HrefDirective = void 0;
const directive_1 = require("./abstracts/directive");
const prepare_directive_decor_1 = require("./decorators/prepare-directive.decor");
const router_1 = require("../route/router");
const listeners_collector_1 = require("../providers/listeners-collector");
const app_1 = require("../app");
let HrefDirective = class HrefDirective extends directive_1.Directive {
    constructor(listenersCollector) {
        super();
        this.listenersCollector = listenersCollector;
        this.isListening = false;
        this.canClick = true;
    }
    get baseHref() { return app_1.NimbleApp.instance.baseHref; }
    get hasBaseHref() { return app_1.NimbleApp.instance.hasBaseHref; }
    onRender() {
        var _a;
        this.canClick = false;
        let value = (_a = this.value) !== null && _a !== void 0 ? _a : '';
        let startsWithHash = value.startsWith('#') || value.startsWith('/#');
        let href = value.replace(/^(#)/g, '');
        if (!value.startsWith('http:') && !value.startsWith('https:')) {
            if (router_1.Router.useHash) {
                if (!startsWithHash)
                    href = '#/' + href.replace(/^(\/)/g, '');
                else
                    console.error(`The link "#${href}" with "#" not work in useHash mode setted in NimbleApple.`);
            }
            else {
                this.canClick = true;
                if (startsWithHash) {
                    href = `${location.pathname}#${href}`;
                }
                else if (!href.startsWith('/')) {
                    let prefix = this.baseHref;
                    href = `${prefix.replace(/(\/)$/g, '')}/${href}`;
                }
                if (!this.isListening) {
                    this.isListening = true;
                    this.listenersCollector.subscribe(this.element, 'click', (e) => {
                        if (this.canClick) {
                            let attr = this.element.attributes[this.selector];
                            let href = attr === null || attr === void 0 ? void 0 : attr.value;
                            if (!href || href.startsWith(this.baseHref)) {
                                setTimeout(() => {
                                    router_1.Router.redirect(href);
                                });
                                e.preventDefault();
                            }
                        }
                    }, true);
                }
            }
        }
        if (!this.element.hasAttribute(this.selector))
            this.element.setAttribute(this.selector, href);
        else
            this.element.attributes[this.selector].value = href;
    }
    onChange() {
        this.onRender();
    }
    onDestroy() {
        this.isListening = false;
    }
};
HrefDirective = __decorate([
    prepare_directive_decor_1.PrepareDirective({
        selector: 'href'
    }),
    __metadata("design:paramtypes", [listeners_collector_1.ElementListenersCollector])
], HrefDirective);
exports.HrefDirective = HrefDirective;
//# sourceMappingURL=href.directive.js.map
"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ForDirective = void 0;
const iteration_directive_1 = require("../abstracts/iteration-directive");
const render_abstract_1 = require("../../render/render-abstract");
const prepare_iterate_directive_decor_1 = require("../decorators/prepare-iterate-directive.decor");
const util_1 = require("util");
let ForDirective = class ForDirective extends iteration_directive_1.IterationDirective {
    constructor() {
        super();
        this.last = {
            iteration: [],
            data: [],
        };
    }
    onRender() {
    }
    onIterate() {
        let expression = this.value.trim();
        if (expression.startsWith('(') && expression.endsWith(')')) {
            expression = expression.substr(1, expression.length - 2);
        }
        const startWithVariable = expression.startsWith('let ') || expression.startsWith('var ') || expression.startsWith('const ');
        const iterationVarName = expression.split(' ')[startWithVariable ? 1 : 0];
        const iterationArray = {
            expressionOrName: expression.split(' ').slice(startWithVariable ? 3 : 2).join(''),
            value: this.compile(expression.split(' ').slice(startWithVariable ? 3 : 2).join(''))
        };
        if (!util_1.isArray(iterationArray.value)) {
            this.element.remove();
            console.error(`SyntaxError: Invalid expression: ${iterationArray.expressionOrName} does not appear to be an array.`);
            return [];
        }
        let existingVarNameBefore = false;
        let varValueBefore = null;
        this.last.data = iterationArray.value;
        if (this.last.iteration.length !== iterationArray.value.length) {
            this.last.iteration = iterationArray.value.map((_, i) => new render_abstract_1.IterateDirectiveResponse({
                beginFn: () => {
                    existingVarNameBefore = iterationVarName in this.scope;
                    varValueBefore = existingVarNameBefore ? this.scope[iterationVarName] : null;
                    this.scope[iterationVarName] = this.last.data[i];
                    this.scope['$index'] = i;
                },
                endFn: () => {
                    delete this.scope['$index'];
                    if (existingVarNameBefore)
                        this.scope[iterationVarName] = varValueBefore;
                    else
                        delete this.scope[iterationVarName];
                }
            }));
        }
        return this.last.iteration;
    }
    onChange() {
    }
    onDestroy() {
    }
};
ForDirective = __decorate([
    prepare_iterate_directive_decor_1.PrepareIterateDirective({
        selector: 'for'
    }),
    __metadata("design:paramtypes", [])
], ForDirective);
exports.ForDirective = ForDirective;
//# sourceMappingURL=for.directive.js.map
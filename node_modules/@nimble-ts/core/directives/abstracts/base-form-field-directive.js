"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BaseFormFieldDirective = void 0;
const directive_1 = require("./directive");
const form_field_1 = require("../../forms/form-field");
const form_1 = require("../../forms/form");
class BaseFormFieldDirective extends directive_1.Directive {
    constructor() {
        super();
    }
    get formField() {
        let directive = (this.selector === 'form-field' || this.selector === 'form-field-name')
            ? this
            : this.others.find(x => x.selector === 'form-field' || x.selector === 'form-field-name');
        let value = directive === null || directive === void 0 ? void 0 : directive.value;
        if (value) {
            if (value instanceof form_field_1.FormField)
                return value;
            else
                return this.form && this.form.get(value);
        }
        return null;
    }
    checkForm() {
        if (!this.form || !(this.form instanceof form_1.Form)) {
            let selector = this.selectors[0];
            console.error(`The "${selector}" directive not apply beacuse directive [form] not setted in form element, but you can do like it: <form [form]="yourForm" ...>`);
            return false;
        }
        return true;
    }
}
exports.BaseFormFieldDirective = BaseFormFieldDirective;
//# sourceMappingURL=base-form-field-directive.js.map
"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FormFieldDirective = void 0;
const prepare_directive_decor_1 = require("../decorators/prepare-directive.decor");
const form_field_1 = require("../../forms/form-field");
const internal_observers_collector_1 = require("../../providers/internal-observers-collector");
const util_1 = require("util");
const base_form_field_directive_1 = require("../abstracts/base-form-field-directive");
const listeners_collector_1 = require("../../providers/listeners-collector");
let FormFieldDirective = class FormFieldDirective extends base_form_field_directive_1.BaseFormFieldDirective {
    constructor(listenerCollector, internalEventsCollector) {
        super();
        this.listenerCollector = listenerCollector;
        this.internalEventsCollector = internalEventsCollector;
    }
    onRender() {
        if (this.checkForm()) {
            if (['form-field', 'form-field-name'].some(x => x === this.selector)) {
                if (this.elementIsValid() && (this.value instanceof form_field_1.FormField || this.formFieldNameSelectorIsValid())) {
                    this.formField.setElement(this.element);
                    if (this.canApplyFormField()) {
                        this.resolveFormFieldSelector();
                        this.resolveFormFieldSpecifications();
                    }
                }
            }
            else if (this.selector === '(valueChange)')
                this.resolveValueChangeSelector();
        }
    }
    onChange() {
        if (['form-field', 'form-field-name'].some(x => x === this.selector)) {
            if (this.elementIsValid() && (this.value instanceof form_field_1.FormField || this.formFieldNameSelectorIsValid())) {
                this.resolveFormFieldSpecifications();
            }
        }
    }
    onDestroy() {
    }
    formFieldNameSelectorIsValid() {
        if (typeof this.value !== 'string') {
            console.error('Some form-field-name directive cannot be appplied, because the it value must be a string.');
            return false;
        }
        if (!this.form.has(this.value)) {
            console.error(`The form-field-name directive cannot be appplied, because the "${this.value}" field not exists in the form.`);
            return false;
        }
        return true;
    }
    resolveFormFieldSpecifications() {
        var _a, _b;
        if (this.formField) {
            if (this.formField.isEnabled) {
                (_a = this.element) === null || _a === void 0 ? void 0 : _a.removeAttribute('disabled');
            }
            else {
                (_b = this.element) === null || _b === void 0 ? void 0 : _b.setAttribute('disabled', 'true');
            }
        }
    }
    resolveFormFieldSelector() {
        let field = this.formField;
        let input = this.element;
        if (input.type === 'radio') {
            if (input.value === field.value || (!input.value && !field.value))
                input.checked = true;
            this.listenerCollector.subscribe(this.element, 'click', (e) => {
                if (this.formField.isEnabled) {
                    field.elements.forEach((el) => {
                        if (el !== input)
                            el.checked = false;
                    });
                    input.checked = true;
                    field.setValue(input.value);
                }
            }, true);
        }
        else if (input.type === 'checkbox') {
            let value = input.hasAttribute('value') ? input.value : '';
            if (!util_1.isBoolean(field.value))
                input.checked = (value === field.value) || (util_1.isArray(field.value) && field.value.some(x => x === value));
            else
                input.checked = field.value;
            this.listenerCollector.subscribe(this.element, 'click', (e) => {
                if (this.formField.isEnabled) {
                    if (field.elements.length === 1) {
                        if (!util_1.isBoolean(field.value))
                            field.setValue(input.checked ? value : '');
                        else
                            field.setValue(input.checked);
                    }
                    else {
                        if (field.elements.every(x => x.hasAttribute('value'))) {
                            let values = util_1.isArray(field.value) ? field.value : [field.value];
                            if (input.checked)
                                values.push(value);
                            else
                                values = values.filter(x => x !== value);
                            field.setValue(values);
                        }
                        else {
                            console.error('The [form-field] input of type "checkbox" must contain the attribute "value"');
                        }
                    }
                }
            }, true);
        }
        else {
            input.value = !util_1.isUndefined(field.value) ? field.value : null;
            this.listenerCollector.subscribe(input, 'input', (e) => {
                if (this.formField.isEnabled) {
                    let value = input.value;
                    if (input.type === 'number')
                        value = !isNaN(parseInt(value)) ? parseInt(value) : null;
                    field.setValue(value, { noUpdateElement: true });
                }
            }, true);
        }
    }
    resolveValueChangeSelector() {
        if (this.formField) {
            this.internalEventsCollector.add(this.scope, '(valueChanges)', this.formField.valueChanges.subscribe((value) => {
                this.outputs.valueChanges(value);
            }));
        }
        else {
            console.error('The directive (valueChanges) cannot be appplied, because the [form-field] directive must exist before it.');
        }
    }
    canApplyFormField() {
        return !this.others.some(x => x instanceof base_form_field_directive_1.BaseFormFieldDirective);
    }
    elementIsValid() {
        return [
            HTMLInputElement,
            HTMLTextAreaElement,
            HTMLSelectElement
        ].some(type => this.element instanceof type);
    }
};
FormFieldDirective = __decorate([
    prepare_directive_decor_1.PrepareDirective({
        selector: [
            '[form-field]',
            'form-field-name',
            '(valueChange)'
        ]
    }),
    __metadata("design:paramtypes", [listeners_collector_1.ElementListenersCollector,
        internal_observers_collector_1.InternalObserversCollector])
], FormFieldDirective);
exports.FormFieldDirective = FormFieldDirective;
//# sourceMappingURL=form-field.directive.js.map
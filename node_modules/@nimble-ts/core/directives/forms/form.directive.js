"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FormDirective = void 0;
const directive_1 = require("../abstracts/directive");
const prepare_directive_decor_1 = require("../decorators/prepare-directive.decor");
const listeners_collector_1 = require("../../providers/listeners-collector");
const util_1 = require("util");
let FormDirective = class FormDirective extends directive_1.Directive {
    constructor(listenersCollector) {
        super();
        this.listenersCollector = listenersCollector;
        this.subscribes = [];
    }
    get form() { return this.value; }
    onRender() {
        if (this.isValid()) {
            this.resolve();
            this.onSubmitOutput();
            this.resolveRenderOnInteractSelector();
        }
    }
    onChange() {
    }
    onDestroy() {
        for (let sub of this.subscribes)
            sub.observation.unsubscribe();
    }
    isValid() {
        if (!(this.element instanceof HTMLFormElement)) {
            console.error(`The directive "${this.selector}" only applies in form elements.`);
            return false;
        }
        return true;
    }
    resolve() {
        this.form.scope = this.scope;
        this.form.formElement = this.element;
        this.checkSubmitDirective();
    }
    onSubmitOutput() {
        if (this.outputs.submit && !this.subscribes.find(x => x.id === 'fromDirective')) {
            this.subscribes.push({
                id: 'fromDirective',
                observation: this.form.onSubmit.subscribe((e) => {
                    this.outputs.submit(e);
                    e.preventDefault();
                })
            });
        }
    }
    checkSubmitDirective() {
        if (!this.outputs.submit && this.listenersCollector.getSubscribedsByTargetAndEventName(this.element, 'submit').length <= 1) {
            if (!this.subscribes.find(x => x.id === 'internal')) {
                this.subscribes.push({
                    id: 'internal',
                    observation: this.form.onSubmit.subscribe((e) => {
                        e.preventDefault();
                    })
                });
            }
        }
    }
    resolveRenderOnInteractSelector() {
        let value = this.inputs.renderOnInteract;
        this.form.renderOnInteract = (typeof value === 'boolean') ? value : !!(util_1.isNullOrUndefined(value) || value === '');
    }
};
FormDirective = __decorate([
    prepare_directive_decor_1.PrepareDirective({
        selector: '[form]',
        inputs: ['render-on-interact'],
        outputs: ['submit'],
    }),
    __metadata("design:paramtypes", [listeners_collector_1.ElementListenersCollector])
], FormDirective);
exports.FormDirective = FormDirective;
//# sourceMappingURL=form.directive.js.map
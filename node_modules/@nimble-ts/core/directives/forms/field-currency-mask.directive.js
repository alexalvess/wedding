"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FieldCurrencyMaskDirective = void 0;
const prepare_directive_decor_1 = require("../decorators/prepare-directive.decor");
const util_1 = require("util");
const helper_1 = require("../../providers/helper");
const base_form_field_directive_1 = require("../abstracts/base-form-field-directive");
const listeners_collector_1 = require("../../providers/listeners-collector");
let FieldCurrencyMaskDirective = class FieldCurrencyMaskDirective extends base_form_field_directive_1.BaseFormFieldDirective {
    constructor(helper, listenerCollector) {
        super();
        this.helper = helper;
        this.listenerCollector = listenerCollector;
        this.options = {
            prefix: '',
            decimal: ',',
            thousands: '.',
            precision: 2,
        };
    }
    get prefix() { var _a; return (_a = this.options.prefix) !== null && _a !== void 0 ? _a : ''; }
    get decimalSymbol() { var _a; return (_a = this.options.decimal) !== null && _a !== void 0 ? _a : ','; }
    get thousandsSymbol() { var _a; return (_a = this.options.thousands) !== null && _a !== void 0 ? _a : '.'; }
    get precision() { var _a; return (_a = this.options.precision) !== null && _a !== void 0 ? _a : 2; }
    onRender() {
        if (this.checkForm()) {
            if (this.elementIsValid()) {
                try {
                    this.defineOptions();
                    this.checkValueOnInitialize();
                    this.listenerCollector.subscribe(this.element, 'keypress', this.onKeypress.bind(this), true);
                    this.listenerCollector.subscribe(this.element, 'input', this.onInput.bind(this), true);
                }
                catch (e) {
                    console.error(e.message);
                }
            }
        }
    }
    onChange() {
        this.defineOptions();
        this.checkValueOnInitialize();
    }
    defineOptions() {
        if (util_1.isObject(this.value))
            Object.assign(this.options, this.value);
        else if (!util_1.isNullOrUndefined(this.value))
            this.options.prefix = this.value.toString();
    }
    checkValueOnInitialize() {
        let element = this.element;
        let value = (this.formField) ? this.formField.value : element.value.replace(/[^\d]/g, '');
        value = this.applyMask(value, false);
        element.value = value;
        let floatValue = this.parseFloat(value);
        if (this.formField && floatValue !== this.formField.value)
            this.formField.setValue(floatValue, { noNotify: true, noUpdateElement: true, noValidate: true });
    }
    elementIsValid() {
        if (!(this.element instanceof HTMLInputElement) || this.element.type !== 'text') {
            console.error(`The directive "${this.selector}" only applies in input with type="text".`);
            return false;
        }
        return true;
    }
    onKeypress(event) {
        let element = this.element;
        let nextDigit = event.key;
        let currentValue = element.value;
        let nextValue = currentValue;
        let startIndex = element.selectionStart;
        let endIndex = element.selectionEnd;
        if (nextDigit.length === 1) {
            if (!(/^\d+$/.test(nextDigit)))
                event.preventDefault();
        }
    }
    onInput(event) {
        let element = this.element;
        let value = element.value;
        value = this.applyMask(value);
        element.value = value;
        let floatValue = this.parseFloat(value);
        if (this.formField && floatValue !== this.formField.value)
            this.formField.setValue(floatValue, { noUpdateElement: true });
    }
    applyMask(value, fromTyping = true) {
        value = value ? value : 0;
        if (typeof value === 'string') {
            if (fromTyping) {
                value = value.replace(/([^0-9]*)/g, '').replace(/^0+/g, '');
            }
            else {
                let expression = this.prefix ? `([${this.prefix}| ]*)` : '([ ]*)';
                value = value.toString().replace(new RegExp(expression, 'g'), '');
                value = this.parseFloat(value);
            }
        }
        value = value.toString();
        let integer = '';
        let decimal = '';
        if (value.includes('.')) {
            integer = value.split('.')[0];
            decimal = this.helper.padRight(value.split('.')[1], this.precision);
        }
        else if (!fromTyping) {
            integer = value;
        }
        else {
            integer = value.substr(0, value.length - this.precision);
            decimal = value.length > this.precision ? value.substr(decimal.length - this.precision) : value;
        }
        integer = integer ? integer.replace(/(\d)(?=(?:[0-9]{3})+\b)/g, (v) => `${v}${this.thousandsSymbol}`) : '0';
        decimal = this.helper.padLeft(decimal, this.precision);
        value = `${integer}${this.decimalSymbol}${decimal}`;
        value = (this.prefix ? `${this.prefix} ` : '') + value;
        return value;
    }
    parseFloat(value) {
        if (this.prefix)
            value = value.replace(this.prefix, '').trim();
        value = value.replace(new RegExp(`[\/${this.thousandsSymbol}]`, 'g'), '');
        if (this.decimalSymbol !== '.')
            value = value.replace(this.decimalSymbol, '.');
        let floatValue = value !== '' ? parseFloat(value) : null;
        return floatValue;
    }
    onDestroy() {
    }
};
FieldCurrencyMaskDirective = __decorate([
    prepare_directive_decor_1.PrepareDirective({
        selector: ['field-currency-mask']
    }),
    __metadata("design:paramtypes", [helper_1.Helper,
        listeners_collector_1.ElementListenersCollector])
], FieldCurrencyMaskDirective);
exports.FieldCurrencyMaskDirective = FieldCurrencyMaskDirective;
//# sourceMappingURL=field-currency-mask.directive.js.map
import { Directive } from '../abstracts/directive';
import { Form } from '../../forms';
import { ElementListenersCollector } from '../../providers/listeners-collector';
export declare class FormDirective extends Directive {
    private listenersCollector;
    get form(): Form;
    private subscribes;
    constructor(listenersCollector: ElementListenersCollector);
    onRender(): void;
    onChange(): void;
    onDestroy(): void;
    private isValid;
    private resolve;
    private onSubmitOutput;
    private checkSubmitDirective;
    private resolveRenderOnInteractSelector;
}
